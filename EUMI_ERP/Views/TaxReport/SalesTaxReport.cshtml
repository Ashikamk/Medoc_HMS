
@{
    ViewBag.Title = "SalesTaxReport";
}



<script type="text/javascript" src="../Projectscript/Common.js"></script>


<style type="text/css">
    #btnsubmit:hover {
        -ms-transform: scale(1.1);
        transform: scale(1.1);
    }

    #btnback:hover {
        -ms-transform: scale(1.1);
        transform: scale(1.1);
    }

    .form-control {
        font-weight: bold;
    }
</style>

<div class="myData" data-x="M92" /> 

<div id="Entry">
 
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header" style="border-bottom:1px solid #e6e6e6">

                    <div>
                        <table width="100%">
                            <tr>
                                <td><h4 id="header"></h4></td>
                                <td width="44%"></td>
                                <td align="right"><button type="button" style="width:100px" onclick="$('#popupdiv').show(), $('#area').focus()" id="btnback" class="btn btn-outline-primary">Back</button></td>


                                <td align="right">

                                    <div class="btn-group" id="buttonPlace">




                                        <div align="right">
                                            <div class="heading-elements">
                                                <div class="btn-group" id="buttonPlace">

                                                </div>
                                            </div>
                                        </div>

                                    </div>
                                </td>

                            </tr>
                        </table>


                        <div align="right">

                            <div class="heading-elements">




                                <hr style="margin-top:.1%;" />
                                <div id="divreport" style="overflow:auto;display:none">
                                    <table id="Tblreport" class="table table-striped table-bordered base-style dataTable"></table><br />
                                </div>
                                <div style="display:none">
                                    <table id="Tblreportprint"></table>
                                </div>

                              
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>



<div id="popupdiv" class="modal fade text-left show" tabindex="-1" role="dialog" aria-labelledby="myheader" style="padding-right: 17px; background:rgba(0,0,0,0.5); display: block; display:none1;height:200%">
    <div class="modal-dialog" role="document" style="top:5%">
        <div class="modal-content">
            <div class="modal-header bg-primary white">
                <h5 class="modal-title" id="myheader">Sales Tax Report</h5>
                <button type="button" class="close" onclick="popuprefresh()" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">×</span>
                </button>
            </div>
            <form id="monthpopup" style="display:none1">

                <div class="modal-body">
                    <div class="row">
                        <label style="font-weight:bold;margin-left:12%">From</label>
                        <div class="col-md-4">
                            <input type="text" id="DateFrom" class="form-control" autocomplete="off">
                        </div>


                        <label style="font-weight:bold;margin-left:6%">To</label>
                        <div class="col-md-4">
                            <input type="text" id="DateTo" class="form-control" autocomplete="off">
                        </div>
                    </div>
                    <div class="row" style="border-bottom:1px solid #cfd8dc;margin-top:3%;"></div>

                    <div class="row" style="margin-top:2%">
                        <label style="margin-top:2%;font-weight:bold;margin-left:13%">Area</label>
                        <div class="col-md-4" style="margin-top:2%">
                            <select id="area" name="priority" class="form-control"></select>
                        </div>


                        <label style="margin-top:2%;font-weight:bold;margin-left:1%">Dept.</label>
                        <div class="col-md-4" style="margin-top:2%">
                            <select id="dept" name="priority" class="form-control"></select>
                        </div>
                    </div>
                    <div class="row" style="margin-top:2%;display:none1">
                        <label style="margin-top:2%;font-weight:bold;margin-left:4.5%">Tax Group</label>
                        <div class="col-md-4" style="margin-top:2%">
                            <select id="DTaxid" name="priority" class="form-control"></select>
                        </div>

                    </div>

                </div>
           
                <div class="modal-footer">
                    <fieldset class="form-group floating-label-form-group" style="margin-right:10%">
                        <div class="row">
                            <div class="custom-control custom-checkbox">
                                <input type="checkbox" class="custom-control-input" name="customCheck" id="grpbyarea">
                                <label class="custom-control-label" for="grpbyarea" style="font-weight:bold">Group by Area</label>
                            </div>

                        </div>
                    </fieldset>
                    <button type="button" style="width:100px;margin-right:40%" id="btnsubmit" class="btn btn-primary">Go</button>
                </div>
            </form>

        </div>
    </div>
</div>


<script>

    function popuprefresh() {
        $('#popupdiv').hide();
        $('#taxgrp').hide();
        $("#DTaxid").val(DefaultTaxPercentageId);
    }


    function GetAreaGroupselect(id) {
        var data = {};
        data.AreaGrpId = 0;
        $.ajax({
            type: "POST",
            url: "../Master/AreaGroupGetandGets",
            data: data,
            success: function (result) {
                AreaGroupLoad(result.oList, id);
            }
        });

    }

    function AreaGroupLoad(result) {
        $("#area").empty();
        $("#area").append("<option value='0'>-All-</option>");
        for (var i = 0; i < result.length; i++) {
            $("#area").append("<option value='" + result[i].AreaGrpId + "'>" + result[i].AreaName + "</option>");
        }
    }
    function GetDepartment(id) {
        var data = {};
        data.UserId = ERPUserId;
        $.ajax({
            type: "POST",
            url: "../Master/UserDepartmentGetandGets",
            data: data,
            success: function (result) {
                DepartmentLoad(result.oList, id);


            }
        });
    }

    function DepartmentLoad(result, a) {
        $("#dept").empty();
        $("#dept").append("<option value='0'>-All-</option>");
        for (var i = 0; i < result.length; i++) {
            $("#dept").append("<option value='" + result[i].DepartmentId + "'>" + result[i].DepartmentName + "</option>");
        }
    }

    function TaxCall(id) {
        var data1 = {};
        data1.TaxId = 0;
        $.ajax({
            type: "POST",
            url: "../Master/TaxGetandGets",
            data: data1,
            success: function (result) {
                TaxLoad(result.oList, id);
            }
        });
    }

    function TaxLoad(result) {
        $("#DTaxid").empty();
        $("#DTaxid").append("<option value='0'>-All-</option>");
        for (var i = 0; i < result.length; i++) {
            $("#DTaxid").append("<option value='" + result[i].TaxId + "' name='" + result[i].TaxRate + "'>" + result[i].TaxName + "</option>");
        }
        $("#DTaxid").val(DefaultTaxPercentageId);
    }

    function Showreport(result) {
        disable_datatable('Tblreport', 'buttonPlace');
        var responseText = "<thead><tr><th style=width:5px>Sl#</th><th>Bill#</th><th style=text-align:center>Date</th><th>Customer</th><th>Customer VAT#</th><th style=text-align:right>Disc.</th><th style=text-align:right>Taxable Amount</th><th style=text-align:right>Tax Percentage</th><th style=text-align:right>Tax Amount</th><th style=text-align:right>Amount</th></tr></thead><tbody>";
        for (var i = 0; i < result.length; i++) {
            var slno = parseInt(i + 1);
            if (result[i].InvoDate == "Grand Total") {
                responseText += '<tr style=background-color:#cfd8dc><td>' + slno + '</td><td>' + result[i].SlNo + '</td><td><b>' + result[i].InvoDate + '</b></td><td>' + result[i].Customer + '</td><td>' + result[i].CustomerVATNo + '</td><td  style=text-align:right>' + result[i].Discount + '</td><td style=text-align:right><b>' + parseFloat(result[i].TaxableAmount || 0).toFixed(Decimal) + '</b></td><td style=text-align:right><b>' + result[i].TaxPercent + '</b></td><td style=text-align:right><b>' + parseFloat(result[i].TaxAmount || 0).toFixed(Decimal) + '</b></td><td style=text-align:right><b>' + parseFloat(result[i].TotalAmount || 0).toFixed(Decimal) + '</b></td></tr>';
            }
            else if (result[i].SlNo == '')
            {
                responseText += '<tr style=background-color:#cfd8dc><td>' + slno + '</td><td></td><td><b>' + result[i].InvoDate + '</b></td><td>' + result[i].Customer + '</td><td>' + result[i].CustomerVATNo + '</td><td  style=text-align:right>' + result[i].Discount + '</td><td style=text-align:right>' + result[i].TaxableAmount + '</td><td style=text-align:right>' + result[i].TaxPercent + '</td><td style=text-align:right>' + result[i].TaxAmount + '</td><td style=text-align:right>' + result[i].TotalAmount + '</td></tr>';
            }
            else {
                responseText += '<tr ondblclick=gotoInvoNo(' + result[i].InvoNo + "," + result[i].BillSeriesId + "," + result[i].Dept + ')><td>' + slno + '</td><td>' + result[i].SlNo + '-' + result[i].InvoNo + '</td><td>' + result[i].InvoDate + '</td><td>' + result[i].Customer + '</td><td>' + result[i].CustomerVATNo + '</td><td  style=text-align:right>' + parseFloat(result[i].Discount || 0).toFixed(Decimal) + '</td><td style=text-align:right>' + parseFloat(result[i].TaxableAmount || 0).toFixed(Decimal) + '</td><td style=text-align:right>' + parseFloat(result[i].TaxPercent || 0).toFixed(Decimal) + '</td><td style=text-align:right>' + parseFloat(result[i].TaxAmount || 0).toFixed(Decimal) + '</td><td style=text-align:right>' + parseFloat(result[i].TotalAmount || 0).toFixed(Decimal) + '</td></tr>';
            }
        }
        $('#Tblreport').html(responseText + '</tbody><tfoot><tr><th>Sl#</th><th>Bill#</th><th>Date</th><th>Customer</th><th>Customer VAT#</th><th> </th><th> </th><th> </th><th> </th><th> </th></tr></tfoot>');
        $('#Tblreportprint').html(responseText + '</tbody>');
        var title = "Sales Tax Report - from " + $("#DateFrom").val() + " to " + $("#DateTo").val() + "";
        datatableWithsearchs('Tblreport', true, title, 'buttonPlace');
        $('#header').text(title);
    }

   

    function gotoInvoNo(InvoNo, BillSeriesId, DeptId) {
        if ((usermenu1.indexOf("M54") != -1)) {
            window.open('../inventory/SalesInvoice?slno=' + InvoNo + '&billseries=' + BillSeriesId + '&dept=' + DeptId);

        }
        else if ((usermenu1.indexOf("M227") != -1)) {
            window.open('../inventory/SalesInvoiceNew?slno=' + InvoNo + '&billseries=' + BillSeriesId + '&dept=' + DeptId);

        }
        else {
            warningshow('Invalid User Rights');
            return false;
        }
    }
    function warningshow(message, Id) {
        $('#popupmessage').text(message);
        $('#Warningpopup').show();
        $('#' + Id).focus();
        window.setTimeout(function () {
            $('#Warningpopup').hide();
        }, 3000);
    }


    $(function () {
        GetAreaGroupselect(0);
        GetDepartment(0);
        TaxCall(0);
        $('#area').focus();

        $('#DateFrom').daterangepicker({
            minDate: minDate,
            maxDate: new Date(new Date().getFullYear(), new Date().getMonth(), new Date().getDate()),
            singleDatePicker: true,
            showDropdowns: true,
            locale: {
                format: 'DD/MM/YYYY'
            }
        });

        $('#DateTo').daterangepicker({
            minDate: minDate,
            maxDate: new Date(new Date().getFullYear(), new Date().getMonth(), new Date().getDate()),
            singleDatePicker: true,
            showDropdowns: true,
            locale: {
                format: 'DD/MM/YYYY'
            }
        });
        $('#area').keypress(function (e) {
            var key = e.charCode ? e.charCode : e.keyCode ? e.keyCode : 0;
            if (key == 13) {
                $('#dept').focus();
                e.preventDefault();
            }
        });
        $('#dept').keypress(function (e) {
            var key = e.charCode ? e.charCode : e.keyCode ? e.keyCode : 0;
            if (key == 13) {
                $('#btnsubmit').focus();
                e.preventDefault();
            }
        });

        $("#btnsubmit").click(function (e) {



            if ($('#DateFrom').val() == '') {
                alert('Please Select From Date');
                $('#DateFrom').focus();

            }
            else if ($('#DateTo').val() == '') {
                alert('Please Select To Date');
                $('#DateTo').focus();

            }

            else {


                $('#Loading').show();


                if ($("#grpbyarea").prop("checked"))
                    {
                    var data = {};
                    data.FromDate = $('#DateFrom').val();
                    data.ToDate = $('#DateTo').val();
                    data.Dept = parseInt($('#dept').val() || 0);
                    data.Area = parseInt($('#area').val() || 0);
                    data.TaxPercent = parseInt($('#DTaxid').val() || 0);
                    data.Userid = ERPUserId;
                    $.ajax({
                        type: "POST",
                        url: "../TaxReport/SalesTaxReportGet",
                        data: data,
                        success: function (result) {
                            $('#Loading').hide();
                            $('#divreport').show();
                            $('#popupdiv').hide();
                            $('#taxgrp').hide();
                           // $("#DTaxid").val(DefaultTaxPercentageId);
                            Showreport(result);
                        }
                    });

                }
                else
                {
                    var data = {};
                    data.FromDate = $('#DateFrom').val();
                    data.ToDate = $('#DateTo').val();
                    data.Dept = parseInt($('#dept').val() || 0);
                    data.Area = parseInt($('#area').val() || 0);
                    data.TaxPercent = parseInt($('#DTaxid').val() || 0);
                    data.Userid = ERPUserId;
                    $.ajax({
                        type: "POST",
                        url: "../TaxReport/SalesAreaGrpTaxReportGet",
                        data: data,
                        success: function (result) {
                            $('#Loading').hide();
                            $('#divreport').show();
                            $('#popupdiv').hide();
                            $('#taxgrp').hide();
                           // $("#DTaxid").val(DefaultTaxPercentageId);
                            Showreport(result);
                        }
                    });
                }

            }


        });

    });
    $(document).keydown(function (e) {

        if (e.altKey && e.keyCode == 86) {              //ALT+V
            $('#taxgrp').show();
        }
    });

    function datatableWithsearchs(tablename, download, title, tableButtonContainerId) {

        var a = '#' + tablename + ' tfoot th'
        $(a).each(function () {
            var title = $(this).text();
            if (title != 'Edit' && title != 'Select' && title != 'Sl#' && title != 'O Balance' && title != ' ') {
                $(this).html('<input type="text" placeholder=" ' + title + '" style="width=100%" />');
            }
            if (title == 'Date') {
                $(this).html('<input type="text" placeholder="' + title + '" style="width:100%" />');
            }
            if (title == 'Bill#' ) {
                $(this).html('<input type="text" placeholder="' + title + '" style="width:100%" />');
            }
            if (title == 'Customer VAT#') {
                $(this).html('<input type="text" placeholder="' + title + '" style="width:300px" />');
            }
          
            if (title == 'Customer') {
                $(this).html('<input type="text" placeholder="' + title + '" style="width:500px" />');
            }
            if (title == 'Disc.') {
                $(this).html('<input type="text" placeholder="' + title + '" style="width:100%" />');
            }

        });
        var table = null;
        if (download) {
            if (!title || !tableButtonContainerId) { console.log("download table need title and button container"); }


            table = $('#' + tablename).DataTable({
                dom: "<'row'<'col-sm-1'l><'col-sm-11'f>>" +
               "<'row'<'col-sm-12'tr>>" +
               "<'row'<'col-sm-1'i><'col-sm-11'p>>",
                buttons: [],
               

            });
            new $.fn.dataTable.Buttons(table, {
                buttons: [
                {
                    extend: 'collection',
                    text: 'Export',
                    className: 'excelexport',
                    buttons: [
                        {
                            extend: 'excelHtml5',
                            title: title,
                            messageTop: 'MEDOC HMS',
                            exportOptions: { columns: ":visible" }
                        },
                        {
                            extend: 'pdfHtml5',
                            title: title,
                            messageTop: 'MEDOC HMS',
                            exportOptions: { columns: ":visible" }
                        },
                        {
                            extend: 'print',
                            title: title,
                            messageTop: 'MEDOC HMS',
                            exportOptions: { columns: ":visible" }
                        }
                    ]
                },
                'colvis'
                ]
            });
            table.buttons(0, null).container().appendTo($("#" + tableButtonContainerId));
            $("#" + tableButtonContainerId).find(".buttons-collection").addClass("btn-outline-primary mx-1").css("border-radius", "4px");

        } else {
            table = $('#' + tablename).DataTable();
        }
        table.columns().every(function () {
            var that = this;
            $('input', this.footer()).on('keyup change', function () {
                if (that.search() !== this.value) {
                    that
                        .search(this.value)
                        .draw();

                }
            });
        });
        if (ExcelExport == 0) {
            $('.excelexport').hide();
        }
    }

</script>






