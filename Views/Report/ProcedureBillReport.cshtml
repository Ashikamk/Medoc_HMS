
@{
    ViewBag.Title = "ProcedureBillReport";
}


<script type="text/javascript" src="~/ProjectScript/ReportPrint.js"></script>
<script type="text/javascript" src="../Projectscript/Common.js"></script>
<script src="~/Scripts/Autocomplete/jquery-ui.js"></script>

<style>
    .form-control {
        font-weight: bold;
    }

    #btnsubmit:hover {
        -ms-transform: scale(1.1);
        transform: scale(1.1);
    }

    #btnback:hover {
        -ms-transform: scale(1.1);
        transform: scale(1.1);
    }

    #btnprint:hover {
        -ms-transform: scale(1.1);
        transform: scale(1.1);
    }
</style>

<div class="myData" data-x="M365" />
<div id="Entry">

    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header" style="border-bottom:1px solid #e6e6e6">
                    <div>


                        <table style="width:100%">
                            <tr>
                                <td><h4 id="header"></h4></td>
                                <td width="15%"></td>
                                <td align="right">

                                    <button type="button" style="width:100px;margin-left:20px" id="btnprint" onclick="PrintthisReportDOS()" class="btn btn-primary">Print <i class="ft-printer"></i></button>

                                    <button type="button" style="width:100px" onclick="$('#popupdiv').show(), $('#User').focus()" id="btnback" class="btn btn-outline-primary">Back</button>
                                </td>
                                <td style="display:none"><button type="button" style="width:100px;margin-left:20px" id="btnprint" onclick="PrintthisReportWindows('PROCEDUREBillREPORT', $('#LabBillLength').val())" class="btn btn-primary">Print <i class="ft-printer"></i></button></td>

                                <td align="right">

                                    <div class="btn-group" id="buttonPlace">




                                        <div align="right">
                                            <div class="heading-elements">
                                                <div class="btn-group" id="buttonPlace">

                                                </div>
                                            </div>
                                        </div>

                                    </div>
                                </td>

                            </tr>
                        </table>


                        <div align="right">

                            <div class="heading-elements">




                                <div id="divreport" style="display:none; overflow:auto">
                                    <hr style="margin-top:.1%;" />

                                    <table id="Tblreport" class="table table-striped table-bordered dataTable" style="width:100%;margin: 0 auto 0 0;"></table><br />
                                </div>
                                <div style="display:none">
                                    <table id="Tblreportprint"></table>
                                </div>



                            </div>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>
</div>


<div id="popupdiv" class="modal fade text-left show" tabindex="-1" role="dialog" aria-labelledby="myheader" style="padding-right: 17px; background:rgba(0,0,0,0.5); display: block; ">
    <div class="modal-dialog" role="document" style="top:10%">
        <div class="modal-content">
            <div class="modal-header bg-primary white">
                <h3 class="modal-title" id="myheader">Procedure Bill Report</h3>
                <button type="button" class="close" onclick="popuprefresh()" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">×</span>
                </button>
            </div>
            <form id="monthpopup" >
                <div class="modal-body">
                    <div class="row">
                        <label style="font-weight:bold;margin-left:11%">From</label>
                        <div class="col-md-4">
                            <input type="text" id="DateFrom" class="form-control" autocomplete="off">
                        </div>


                        <label style="font-weight:bold;margin-left:4%">To</label>
                        <div class="col-md-4">
                            <input type="text" id="DateTo" class="form-control" autocomplete="off">
                        </div>
                    </div>
                    <div class="row" style="border-bottom:1px solid #cfd8dc;margin-top:3%;"></div>
                    <div class="row" style="margin-top:2%">

                        <label style="margin-top:2%;font-weight:bold;margin-left:8%">Doctor</label>
                        <div class="col-md-9" style="margin-top:2%">
                            <select type="text" id="Doctor" maxlength="100" class="form-control"></select>
                        </div>
                    </div>
                    <div class="row" style="margin-top:2%">
                        <label style="margin-top:2%;font-weight:bold;margin-left:8%">User&#160;&#160;&#160;&#160;&#160;</label>
                        <div class="col-md-9" style="margin-top:2%">
                            <select type="text" id="UserId" maxlength="100" class="form-control"></select>
                        </div>
                    </div>


                    <div class="row" style="margin-top:2%">
                        <label style="margin-top:2%;font-weight:bold;margin-left:8%">Group&#160;&#160;</label>
                        <div class="col-md-9" style="margin-top:2%">
                            <select type="text" id="RefId" maxlength="100" class="form-control"></select>
                        </div>
                    </div>

                </div>

                <div class="modal-footer">
                   
                    <table width="100%">
                        <tr><td align="left">
    <div class="custom-control custom-checkbox">
        <input type="checkbox"  class="custom-control-input" name="customCheck" id="grpbyall" autocomplete="off">
        <label class="custom-control-label" for="grpbyall" style="font-weight:bold">Day Wise</label>
    </div>
</td>
                            <td align="right">
                                <button type="button" style="width:100px;margin-right:6%" id="btnsubmit" class="btn btn-primary">Go</button>
                                <input type="hidden" id="LabBillLength" class="form-control" />
                            </td>
                        </tr>
                    </table>

                   
                </div>
            </form>

        </div>
    </div>
</div>

<div style="display:none">
    <table id="DosPrintVEM" class="dataTableVEM"></table>
</div>

<script>
    function popuprefresh() {
        $('#popupdiv').hide();
    }

    function PrintthisReportDOS() {

        var titles = [];
        var data = [];

        $('.dataTableVEM th').each(function () {
            titles.push($(this).text());
        });
        $('.dataTableVEM tr').each(function () {
            data.push($(this).text());
        });


        var CSVString = prepCSVRow(titles, titles.length, '');
        CSVString = prepCSVRow(data, titles.length, CSVString);
        //console.log(CSVString)
        var downloadLink = document.createElement("a");
        var blob = new Blob(["\ufeff", CSVString]);
        var url = URL.createObjectURL(blob);
        downloadLink.href = url;
        downloadLink.download = "printbill.txt";
        document.body.appendChild(downloadLink);
        downloadLink.click();
        document.body.removeChild(downloadLink);

    }


    function prepCSVRow(arr, columnCount, initial) {
        var row = ''; // this will hold data
        var delimeter = ''; // data slice separator, in excel it's `;`, in usual CSv it's `,`
        var newLine = '\r\n'; // newline separator for CSV row
        function splitArray(_arr, _count) {
            var splitted = [];
            var result = [];
            _arr.forEach(function (item, idx) {
                if ((idx + 1) % _count === 0) {
                    splitted.push(item);
                    result.push(splitted);
                    splitted = [];
                } else {
                    splitted.push(item);
                }
            });
            return result;
        }
        var plainArr = splitArray(arr, columnCount);
        plainArr.forEach(function (arrItem) {
            arrItem.forEach(function (item, idx) {
                row += item + ((idx + 1) === arrItem.length ? '' : delimeter);
            });
            row += newLine;
        });
        return initial + row;
    }




   

    


    $(document).ready(function () {
    DoctorLoad();
    UserList();
    GetRows(0)


        
        function GetRows(HospitalId) {
            var data = {};
            data.HospitalId = HospitalId;
            $.ajax({
                type: "POST",
                url: "../Master/HospitalGetandGets",
                data: data,
                success: function (result) {
                    ShowTermlist(result.oList);
                }
            });

        }


        function ShowTermlist(result) {
            $("#RefId").append("<option value='0'>All</option>");
            for (var i = 0; i < result.length; i++) {
                $("#RefId").append("<option value='" + result[i].HospitalId + "'>" + result[i].HospitalName + "</option>");
            }
        }
  


        function UserList() {

            var data = {};
            data.UserId = 0;
            $.ajax({
                type: "POST",
                url: "../Master/UsersGetandGets",
                data: data,
                success: function (result) {
                    ShowUserslist(result.oList);
                }
            });

        }


        function ShowUserslist(result) {
            $("#UserId").append("<option value='0'>All</option>");
            for (var i = 0; i < result.length; i++) {

                $("#UserId").append("<option value='" + result[i].UserId + "'>" + result[i].UserName + "</option>");

            }

        }
       

       

        function Showreport(result) {

            var space = "                                           ";
            var title = "Procedure Bill Report - from " + $("#DateFrom").val() + " to " + $("#DateTo").val() + "";

            var Line = "<tr><th></th></tr>";

            Line += "<tr><td>" + $.trim(CompanySettingsArray.CompanyName) + "</td></tr>";
            Line += "<tr><td>" + $.trim(CompanySettingsArray.Address) + "</td></tr>";
            Line += "<tr><td>" + $.trim((CompanySettingsArray.PhoneNo).replace("\n", "")) + "</td></tr>";

            Line += '<tr><td>-----------------------------------------------------------------------</td></tr>'   //bold on
            Line += "<tr><td>"  + title + "</td></tr>";
            Line += '<tr><td>-----------------------------------------------------------------------------------------------------</td></tr>'
            Line += '<tr><td>Inv# MR No#    INV No#   Date      Patient                       Amount         Dis     Total        </td></tr>'
            Line += '<tr><td>-----------------------------------------------------------------------------------------------------</td></tr>'

        disable_datatable('Tblreport', 'buttonPlace');
            var responseText = "<thead><tr><th align= center>Sl#</th><th>Bill No</th><th>Reg No</th><th>IP No</th><th>Bill Date</th> <th>Time</th> <th>Patient</th><th>Doctor</th><th>Cash</th> <th>UPI</th> <th>Card</th> <th style='text-align:right;'> Amount</th><th style='text-align:right;'> Discount</th><th style='text-align:right;'>Total Amount</th></tr></thead><tbody>";
        for (var i = 0; i < result.length; i++) {
            var slno = parseInt(i + 1);

            var Taxable = space + parseFloat(result[i].NetAmt).toFixed(Decimal);
            Taxable = Taxable.substring(Taxable.length - 10, Taxable.length);

           
            var Dis = space + parseFloat(result[i].DiscAmt).toFixed(Decimal);
            Dis = Dis.substring(Dis.length - 10, Dis.length);
            var Amt = space + parseFloat(result[i].NetAmt).toFixed(Decimal);
            Amt = Amt.substring(Amt.length - 10, Amt.length);

            var SSSNo = space + slno;
            SSSNo = SSSNo.substring(SSSNo.length - 3, SSSNo.length);
            var spstring = (result[i].Department).split("##");

            if (result[i].BillNo == '0') {
                responseText += '<tr style=background-color:#d3d3d3><td align= center id = Col1_' + slno + '>' + slno + '</td><td id= Col5_' + slno + '>' + " " + '</td><td id= Col6_' + slno + '>' + " " + '</td><td  id= Col7_' + slno + '>' + " " + '</td><td id= Col2_' + slno + '>' + result[i].BillDate + '</td><td></td><td id= Col3_' + slno + '><b>' + result[i].Name + '</b></td><td id= Col4_' + slno + '>' + result[i].Hospital + '</td><td align=right id= Col8_' + slno + '><b>' + spstring[0] + '</b></td><td align=right id= Col8_' + slno + '><b>' + spstring[1] + '</b></td><td align=right id= Col8_' + slno + '><b>' + spstring[2] + '</b></td><td align= right id= Col10_' + slno + '><b>' + parseFloat(result[i].NetAmt).toFixed(Decimal) + '</b></td><td align=right id= Col9_' + slno + '><b>' + parseFloat(result[i].DiscAmt).toFixed(Decimal) + '</b></td><td align = right  id= Col11_' + slno + '><b>' + parseFloat(result[i].TotalAmt).toFixed(Decimal) + '</b></td></tr>';
                Line += '<tr><td>-----------------------------------------------------------------------------------------------------</td></tr>'
                Line += "<tr><td>"  + "                                   " + result[i].BillDate + "   " + (result[i].Name + space).substring(0, 20) + "   " + Taxable + "   " + Dis + "  " + Amt + "</td></tr>";
                Line += '<tr><td>-----------------------------------------------------------------------------------------------------</td></tr>'

            }
            else {
              


                
                responseText += '<tr ><td align= center id = Col1_' + slno + '>' + slno + '</td><td id= Col5_' + slno + '>' + result[i].BillNo + '</td><td id= Col6_' + slno + '>' + result[i].RegNo + '</td><td  id= Col7_' + slno + '>' + result[i].IpNo + '</td><td id= Col2_' + slno + '>' + result[i].BillDate + '</td> <td  id= Col81_' + slno + '>' + spstring[3] + '</td>   <td id= Col3_' + slno + '>' + result[i].Name + '</td><td id= Col4_' + slno + '>' + result[i].Hospital + '</td><td align=right id= Col8_' + slno + '>' + spstring[0] + '</td><td align=right id= Col8_' + slno + '>' + spstring[1] + '</td><td align=right id= Col8_' + slno + '>' + spstring[2] + '</td><td align= right id= Col10_' + slno + '>' + parseFloat(result[i].NetAmt).toFixed(Decimal) + '</td><td align=right id= Col9_' + slno + '>' + parseFloat(result[i].DiscAmt).toFixed(Decimal) + '</td><td align = right  id= Col11_' + slno + '>' + parseFloat(result[i].TotalAmt).toFixed(Decimal) + '</td></tr>';
                Line += "<tr><td>" + SSSNo + "  " + (result[i].RegNo + space).substring(0, 10) + "" + (result[i].BillNo + space).substring(0, 8) + "  " + result[i].BillDate + "   " + (result[i].Name + space).substring(0, 20) + "   " + Taxable + "   " + Dis + "  "+ Amt + "</td></tr>";

            }
        }
            $('#Tblreport').html(responseText + '</tbody><tfoot><tr><th> </th> <th> </th>  <th> </th>  <th> </th>  <th> </th> <th> </th><th> </th><th> </th><th>Patient</th><th>Doctor</th><th> </th><th> </th><th> </th><th> </th></tr> </tfoot>');
        $('#Tblreportprint').html(responseText + '</tbody>');
        var title = "Procedure Bill Report - from " + $("#DateFrom").val() + " to " + $("#DateTo").val() + "";
        datatableWithsearch1('Tblreport', true, title, 'buttonPlace');
        $('#LabBillLength').val(result.length);
            $('#header').text(title);
            $('#DosPrintVEM').html("");
            $('#DosPrintVEM').append(Line);


    }


    $(function () {

        $('#User').focus();
        $('#Dept').keydown(function (e) {
            var key = e.charCode ? e.charCode : e.keyCode ? e.keyCode : 0;
            if (key == 13) {
                $('#btnsubmit').focus();
                e.preventDefault();
            }
        });
        $('#DateFrom').daterangepicker({
            minDate: minDate,
            maxDate: new Date(new Date().getFullYear()+1, new Date().getMonth(), new Date().getDate()),
            singleDatePicker: true,
            showDropdowns: true,
            locale: {
                format: 'DD/MM/YYYY'
            }
        });

        $('#DateTo').daterangepicker({
            minDate: minDate,
            maxDate: new Date(new Date().getFullYear()+1, new Date().getMonth(), new Date().getDate()),
            singleDatePicker: true,
            showDropdowns: true,
            locale: {
                format: 'DD/MM/YYYY'
            }
        });
        $('#DateFrom,#DateTo').val(CurDate);
    });
        $("#btnsubmit").click(function (e) {

            var DFlag = 0;
            if ($("#grpbyall").prop("checked")) {
                DFlag = 1;
            }

            if ($('#DateFrom').val() == '') {
                alert('Please Select From Date');
                $('#DateFrom').focus();

            }
            else if ($('#DateTo').val() == '') {
                alert('Please Select To Date');
                $('#DateTo').focus();

            }

            else {
                $('#Loading').show();

                var data = {};
                data.FromDate = $('#DateFrom').val() + '##' + DFlag;
                data.ToDate = $('#DateTo').val() + '##' + $('#RefId').val();
                data.Doctor = parseInt($('#Doctor').val() || 0);
                data.UserId = parseInt($('#UserId').val() || 0);

                $.ajax({
                    type: "POST",
                    url: "../Master/ProcedureBillReport",
                    data: data,
                    success: function (result) {
                        $('#Loading').hide();
                        $('#divreport').show();
                        $('#popupdiv').hide();
                        Showreport(result);


                    }
                });
            }
        });

    });

    function DoctorLoad() {
        var data = {};                                       //dropdownbind
        data.DoctorId = 0;
        data.DeptId = ERPDeptId;
        $.ajax({
            type: "POST",
            url: "../Revisit/HMS_RevistDoctorGets",
            data: data,
            success: function (result) {
                if (result.oList.length > 0) {
                    $("#Doctor").empty();
                    $("#Doctor").append("<option value='0' Fee='0'>Select</option>");
                    for (var i = 0; i < result.oList.length; i++) {
                        $("#Doctor").append("<option value='" + result.oList[i].DoctorId + "' Fee='" + result.oList[i].ConsultFees + "'>" + result.oList[i].DoctorName + "</option>");
                    }
                }
            }
        });
    }

    function datatableWithsearch1(tablename, download, title, tableButtonContainerId) {

        var a = '#' + tablename + ' tfoot th'
        $(a).each(function () {
            var title = $(this).text();
            if (title != ' ')
                $(this).html('<input type="text" placeholder="Search ' + title + '" style="width:100%" />');

        });
        var table = null;
        if (download) {

            if (!title || !tableButtonContainerId) { console.log("download table need title and button container"); }

            // AddColumnSelectionButton(tableButtonContainerId, tablename)

            table = $('#' + tablename).DataTable({
                dom: "<'row'<'col-sm-1'l><'col-sm-11'f>>" +
                            "<'row'<'col-sm-12'tr>>" +
                            "<'row'<'col-sm-1'i><'col-sm-11'p>>",
                buttons: [],
                "columnDefs": [
                 { "width": "0%", "targets": 0 },
                      { "width": "7%", "targets": 1 },
                      { "width": "7%", "targets": 2 },
                      { "width": "7%", "targets": 3 },
                        { "width": "7%", "targets": 4 },
                          { "width": "7%", "targets": 5 },
                      { "width": "20%", "targets": 6 },
                      { "width": "20%", "targets": 7 },
                      { "width": "10%", "targets": 8 },
                        { "width": "5%", "targets": 9 },
                         { "width": "10%", "targets": 10 },



                ],

            });
            new $.fn.dataTable.Buttons(table, {
                buttons: [
                {
                    extend: 'collection',
                    text: 'Export',
                    buttons: [
                        {
                            extend: 'excelHtml5',
                            title: title,                           
                            //exportOptions: { columns: [0, 1, 2, 4, 5, 8, 9, 10] }
                        },
                        {
                            extend: 'pdfHtml5',
                            title: title,
                           
                            //exportOptions: { columns: [0, 1, 2, 4, 5, 8, 9, 10] }
                        },
                        {
                            extend: 'print',
                            title: title,
                            
                            exportOptions: { columns: [0, 1, 2, 4, 5,7,8,9,10,11,12] }
                        }
                    ]
                },
                'colvis'
                ],

            });

            table.buttons(0, null).container().appendTo($("#" + tableButtonContainerId));
            $("#" + tableButtonContainerId).find(".buttons-collection").addClass("btn-outline-primary mx-1").css("border-radius", "4px");



        } else {
            table = $('#' + tablename).DataTable();
        }
        table.columns().every(function () {
            var that = this;
            $('input', this.footer()).on('keyup change', function () {
                if (that.search() !== this.value) {
                    that
                        .search(this.value)
                        .draw();

                }
            });
        });
    }

      

</script>






